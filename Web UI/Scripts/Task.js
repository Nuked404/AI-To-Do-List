export class Task {
  constructor(title, taskType, etaTime, priority, status, dueDate) {
    this.title = title;
    this.taskType = taskType;
    this.etaTime = etaTime;
    this.priority = priority;
    this.status = status;
    this.dueDate = dueDate;
  }
}
